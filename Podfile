# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RControlDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # 资源管理库
  pod 'R.swift'
  # 链式布局库
  pod 'Cupcake', '~> 1.2.0'

  # Pods for RControlDemo

  target 'RControlDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RControlDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
